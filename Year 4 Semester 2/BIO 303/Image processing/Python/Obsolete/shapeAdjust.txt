#
# para: 
#
# output: 
#
#

def shapeAdjust(process):

	def

		
		hsv = cv2.cvtColor(process, cv2.COLOR_BGR2HSV)
		spot, sur = spotEx(hsv, process, 1, rw, cl, hoffset, voffset, hdist, vdist, r, r, th, 0)